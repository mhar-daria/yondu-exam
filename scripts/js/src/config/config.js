// additional ajax header
$.ajaxSetup({
	// headers: {
	// 	'X-CSRF-TOKEN': $('meta[name="s-key"]').attr('content')
	// }
});

window.APP = window.APP || {};

window.APP.config = {

	maps: {

		
	},
};